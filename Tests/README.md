## RegEx tests
Чтобы протестировать модуль регулярных выражений надо
1. Скомпилировать проект
2. В файле `Tests/reTests.py` поменять переменную `testRe` на желаемое регулярное выражение
3. Написать строки, на которых хотите протестировать программу в список `tests`
